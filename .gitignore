# Rust
target/
# Also ignore a root `target` symlink (shared CARGO_TARGET_DIR across worktrees);
# the `target/` rule above matches directories only, not the symlink.
/target
# Keep Cargo.lock for binary/workspace projects (needed for reproducible builds)
# Cargo.lock
**/*.rs.bk
*.pdb

# TypeScript build cache
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Rusternetes specific
*.log
data/
configs/local-*

# Development environment
.env
.env.local
*.local.*

# Podman/Docker volumes
etcd-data/
# Local development runtime state
.rusternetes/

# Work tracking (local only)
.work/

# Node conformance harness
.bin/

*.db*
