name: Conformance canary

# A focused Hydrophone conformance test run against a clean rusternetes
# cluster brought up via `docker compose` inside the
# `arc-runner-set-conformance` scale set (DinD-enabled).
#
# Stack: multi-container SQLite (`compose.sqlite.yml`).
# Storage is rhino (etcd-compatible gRPC server backed by SQLite) instead of
# etcd. rhino's SQLite driver compacts old revisions and reclaims disk on a
# background loop (`auto_vacuum=incremental` + post-compact `incremental_vacuum`),
# so unlike etcd's fixed `--quota-backend-bytes` it does NOT raise a hard
# `NOSPACE` alarm and wedge writes on a long run — a 3h full run against etcd
# hit the 8 GiB quota and failed 206 tests with cascading `context deadline
# exceeded` (etcd compaction frees bbolt pages but never shrinks the file).
# The
# all-in-one stack was tried first (cheaper, ~400-600 MB) but its kube-
# proxy runs inside the same container netns as the rusternetes process
# and writes iptables rules that the host kernel never consults, so any
# ClusterIP-targeting traffic (notably pod -> CoreDNS at 10.96.0.10)
# silently times out. The multi-container stack runs kube-proxy in its
# own container with `network_mode: host` + CAP_NET_ADMIN, which is the
# only configuration we have where ClusterIP DNAT actually fires for
# pod traffic. Cost is ~1.2 GB cluster memory and ~5 min extra startup
# vs the all-in-one stack.
#
# Purpose: prove the ARC → DinD → compose → bootstrap → hydrophone
# pipeline end-to-end, and report the conformance pass/fail delta across
# the full upstream [Conformance] tag set.
#
# Coverage: the canary no longer enforces the ci/conformance/known-green.txt
# ratchet. It now runs the canonical two-phase [Conformance] tag split via
# `scripts/conformance-tags-run.sh` (phase 1: parallel, skipping
# [Slow]/[Serial]/[Flaky]; phase 2: the [Serial]/[Slow] conformance subset,
# single-threaded). This is DISCOVERY/REPORTING, not a gate: rusternetes is
# not yet 100% conformant, so test failures are expected and do NOT fail the
# job — only an infra failure (hydrophone producing no junit) does. Track the
# delta via the printed counts and the uploaded junit, not a red/green job.
#
# Triggers:
#   * workflow_dispatch — manual button on the Actions tab. `image-tag`
#     selects a published GHCR tag (default `main`); empty builds locally.
#   * workflow_run after "Publish images" succeeds on main — the canary
#     checks out and tests the EXACT sha the publish built, so every
#     conformance result maps 1:1 to a main commit. Replaces the old
#     push-paths trigger: publish-images runs on every main push, so the
#     canary now covers every merge, not just CI-file edits (#1056).
#
# Open follow-ups (canary backlog):
#   - [x] Replace etcd with the SQLite backend for the canary (done: now uses
#         compose.sqlite.yml + rhino). This also removes the etcd 8 GiB
#         NOSPACE wedge that capped long runs — see Stack note above.
#   - [x] Retire the known-green.txt ratchet in favour of the two-phase
#         [Conformance] tag split (conformance-tags-run.sh).
#   - [x] Split phase 1 (parallel) and phase 2 (serial) into concurrent matrix
#         legs so neither shares a 300-min step timeout (#1029). The combined
#         single-step run was measured at 300.2 min → hard timeout.

on:
  workflow_dispatch:
    inputs:
      image-tag:
        description: >-
          GHCR image tag to test (e.g. main or sha-<sha>). Empty = build
          images locally from the checked-out ref.
        required: false
        default: main
  workflow_run:
    workflows: ["Publish images"]
    types: [completed]
    branches: [main]

# Newest main merge wins (#1107). The canary fires after every "Publish images"
# run (i.e. every main push), and a run can take hours on the shared
# arc-runner-set-conformance scale set, so without this a busy day queues >24h
# of canary work. Superseded merges' `sha-<sha>` images stay on GHCR, so any
# skipped sha is still re-dispatchable via the `image-tag` workflow_dispatch
# input — cancelling in-progress runs loses no recoverable signal.
concurrency:
  group: conformance-canary
  cancel-in-progress: true

env:
  # The dind sidecar exposes its daemon at this canonical path; DOCKER_HOST
  # points the `docker compose` CLI at it to bring the stack up. The pod
  # runtime itself is the in-compose containerd service, not this socket.
  DOCKER_HOST: unix:///var/run/docker.sock
  # Pin Hydrophone to a known release so a tag-rename upstream doesn't
  # silently break the workflow. The conformance image itself is pinned
  # by `scripts/conformance-tags-run.sh` (defaults to v1.35.0).
  HYDROPHONE_VERSION: v0.7.0

jobs:
  hydrophone:
    name: hydrophone (phase ${{ matrix.phase }})
    # workflow_run fires for failed publishes too — only run on success.
    # workflow_dispatch has no workflow_run payload and always runs.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    # Phase 1 (parallel: [Conformance] minus [Slow]/[Serial]/[Flaky]) and
    # phase 2 (serial: the [Serial]/[Slow] conformance subset) run as two
    # independent matrix legs, each on its own runner + cluster,
    # concurrently. Splitting them (#1029) stops the serial phase's
    # compounding per-test timeouts from eating into phase 1's budget: the
    # old single job ran BOTH phases back-to-back inside ONE 300-min step and
    # routinely hit the ceiling (measured 300.2 min → hard timeout). Each leg
    # now gets the full 300-min budget of its own. If only one DinD runner is
    # free the legs serialize on the runner, but never inside a shared step
    # timeout, so neither phase can starve the other. fail-fast:false so an
    # infra failure in one leg does not cancel the other.
    strategy:
      fail-fast: false
      matrix:
        phase: [1, 2]
    # This job no longer pushes the badge (moved to the `badge` aggregation
    # job below), so it only needs read access.
    permissions:
      contents: read
    # Targets the DinD-enabled scale set. If the scale set isn't
    # deployed on the operator's cluster, this job will sit "queued"
    # forever — operator install of the helm release is a manual
    # one-off (not in this repo by design; see PR #191 policy).
    runs-on: arc-runner-set-conformance
    # Build is ~12 min (Dockerfile.services shared builder, PR #190). A single
    # conformance phase is the long pole: unlike the old known-green subset
    # (322 specs that all PASS quickly), the full [Conformance] tag set
    # includes specs rusternetes does not yet pass, and each FAILING spec
    # burns its per-test ginkgo timeout before moving on — so wall time scales
    # with the failure count, not just the spec count. The 300-min run-step
    # ceiling bounds a wedged phase; the job ceiling clears build + 300 +
    # teardown.
    timeout-minutes: 330

    steps:
      - name: Checkout
        # For workflow_run events, github.sha is the DEFAULT-BRANCH HEAD at
        # trigger time, which may have advanced past the publish — pin to the
        # exact sha the images were built from. Dispatch falls through to the
        # invoking ref (empty ref input = default behavior).
        # rhino is an in-tree submodule (./rhino, indyjonesnl/rhino). The
        # storage crate's Cargo.toml references `path = "../../rhino"` and the
        # compose files expose it as a named build context
        # (`additional_contexts: rhino: ./rhino`). submodules: recursive
        # checks it out at the pinned commit so the compose build finds it.
        uses: actions/checkout@v4
        with:
          submodules: recursive
          ref: ${{ github.event.workflow_run.head_sha || '' }}

      - name: Resolve image tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "IMAGE_TAG=sha-${{ github.event.workflow_run.head_sha }}" >> "$GITHUB_ENV"
          else
            echo "IMAGE_TAG=${{ github.event.inputs.image-tag }}" >> "$GITHUB_ENV"
          fi
          echo "image tag: '$(grep ^IMAGE_TAG "$GITHUB_ENV" | cut -d= -f2-)'"

      - name: Bring up cluster
        uses: ./ci/actions/bring-up-cluster
        with:
          hydrophone-version: ${{ env.HYDROPHONE_VERSION }}
          kubectl-version: v1.35.0
          image-tag: ${{ env.IMAGE_TAG }}
          # workflow_run fires right after "Publish images" succeeded for this
          # exact sha, so sha-<head_sha> MUST be pullable — a miss means the
          # GHCR packages are still private (forgotten visibility flip), which
          # the soft fallback would hide behind a green local build forever
          # (#1108). Hard-fail there; keep the soft fallback for dispatch (where
          # an arbitrary / not-yet-published tag is a legitimate input).
          require-prebuilt: ${{ github.event_name == 'workflow_run' }}

      - name: Run conformance tag suite (phase ${{ matrix.phase }})
        # `scripts/conformance-tags-run.sh --phase N` runs ONE half of the
        # canonical [Conformance] split (phase 1: parallel, skip
        # [Slow]/[Serial]/[Flaky]; phase 2: the [Serial]/[Slow] conformance
        # subset, single-threaded), parses its junit, and prints
        # pass/fail/skip counts. The two phases run as separate matrix legs
        # (#1029) so they no longer share a step timeout. It is NON-GATING:
        # test failures are expected (rusternetes is not yet 100% conformant)
        # and do NOT fail this step — only an infra failure (hydrophone
        # producing no junit) exits non-zero. Pins the conformance image to
        # v1.35.0 by default — see the script's --help. Self-hosted runner, no
        # GH minute cost; the ceiling covers failing specs each burning their
        # per-test timeout (see job timeout note above).
        timeout-minutes: 300
        run: |
          bash scripts/conformance-tags-run.sh \
            --phase ${{ matrix.phase }} \
            --output-dir conformance-out

      - name: Upload conformance artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          # Per-phase name so both legs' artifacts coexist; the badge job
          # downloads them with `pattern: conformance-out-phase*`.
          name: conformance-out-phase${{ matrix.phase }}
          path: conformance-out/
          retention-days: 14

      - name: Tear down cluster
        if: always()
        run: |
          docker compose -f compose.sqlite.yml logs --tail=200 || true
          docker compose -f compose.sqlite.yml down -v || true

  # Aggregate both phases' junit into the single conformance pass-rate badge.
  # A separate job (needs: hydrophone) because the phases now run on
  # independent runners — this pulls both artifacts and sums the counts so the
  # badge still reflects the whole [Conformance] tag set, exactly as the old
  # single-job in-line step did. Runs even if a phase failed/timed out
  # (if: always) so a partial run still refreshes the badge from whatever
  # junit was produced; best-effort, so a missing artifact never blocks.
  badge:
    name: Update conformance badge
    needs: hydrophone
    if: always()
    # contents:write lets update-badge.sh push the latest pass-rate JSON to
    # the orphan `badges` branch.
    permissions:
      contents: write
    # Trivial git/bash work — runs on the light tier, not a scarce DinD runner.
    runs-on: arc-runner-set-small
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download phase artifacts
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          # merge-multiple reconstructs conformance-out/{phase1-parallel,
          # phase2-serial}/junit_01.xml from the two per-phase artifacts.
          path: conformance-out
          pattern: conformance-out-phase*
          merge-multiple: true

      - name: Update conformance badge
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          passed=0; failed=0
          shopt -s nullglob
          for j in conformance-out/*/junit_01.xml; do
            p=$(grep -oE 'status="passed"' "$j" | wc -l | tr -d ' ')
            f=$(grep -oE 'status="failed"' "$j" | wc -l | tr -d ' ')
            passed=$((passed + p)); failed=$((failed + f))
          done
          bash scripts/update-badge.sh conformance "conformance" "$passed" "$((passed + failed))"
