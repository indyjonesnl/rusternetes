# GENERATED by scripts/gen-target-workflows.sh from ci/conformance/targets.json.
# DO NOT EDIT — change the registry and re-run the generator instead.
name: conformance-sig-node

# Independent per-target conformance run. Full runs fire nightly (refreshing the
# sig-node badge against the latest main image); workflow_dispatch with a `focus`
# runs one named test to prove a PR (that focused run does NOT touch the badge).
# All logic lives in the reusable .github/workflows/conformance-target.yml engine.
on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      focus:
        description: "Ginkgo --focus override (narrow to one test to prove a PR). Empty = full target run."
        required: false
        default: ''
      image-tag:
        description: "GHCR image tag to test, or 'local' to build this ref from source (use 'local' to prove a PR)."
        required: false
        default: main

# Per-target group so a newer run supersedes an older one for THIS target
# without cancelling other targets' runs (independence: FR-003).
concurrency:
  group: conformance-sig-node
  cancel-in-progress: true

jobs:
  run:
    permissions:
      contents: write
    secrets: inherit
    uses: ./.github/workflows/conformance-target.yml
    with:
      target: sig-node
      focus: ${{ github.event.inputs.focus || '' }}
      # The engine builds from source when image-tag is EMPTY, but an empty
      # dispatch value cannot reach it: GitHub substitutes this input's own
      # `default: main` for an omitted-or-empty value, so `-f image-tag=''`
      # arrives as 'main'. (An earlier attempt keyed on `== null` for the same
      # reason and still resolved to 'main' -- see the run log line
      # `image-tag: main`.) A literal sentinel is the only value that survives
      # the round trip, so 'local' means "build this ref" and maps to the empty
      # string the engine expects.
      image-tag: ${{ github.event.inputs.image-tag == 'local' && '' || (github.event.inputs.image-tag || 'main') }}
