# Docker Compose for rusternetes with SQLite storage via rhino (no etcd).
#
# Rhino is an etcd-compatible gRPC server backed by SQLite. All components
# use their existing --etcd-servers flag pointed at rhino — no feature flags
# or recompilation needed. The same binaries work with etcd or rhino.
#
# Usage:
#   docker compose -f docker-compose.sqlite.yml build
#   docker compose -f docker-compose.sqlite.yml up -d
#   bash scripts/bootstrap-cluster.sh
#
# Requires the rhino repo adjacent to this one:
#   dev/
#     rusternetes/   (this repo)
#     rhino/         (https://github.com/calfonso/rhino)

services:
  # Rhino - etcd-compatible gRPC server backed by SQLite
  rhino:
    build:
      context: ../rhino
      dockerfile: ../rusternetes/rhino.Dockerfile
    container_name: rusternetes-rhino
    ports:
      - "2379:2379"
    volumes:
      - rhino-data:/data/db
    networks:
      - rusternetes-net
    command:
      - "--listen-address"
      - "0.0.0.0:2379"
      - "--endpoint"
      - "/data/db/state.db"
      - "--max-connections"
      - "20"
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 5s
      timeout: 3s
      retries: 3
      start_period: 2s

  # API Server - central management component
  api-server:
    build:
      context: .
      dockerfile: services.Dockerfile
      target: api-server
    container_name: rusternetes-api-server
    ports:
      - "6443:6443"
    depends_on:
      rhino:
        condition: service_healthy
    networks:
      rusternetes-net:
        aliases:
          - api-server
    volumes:
      - ./.rusternetes/certs:/etc/kubernetes/pki:ro
      - /var/run/docker.sock:/var/run/docker.sock:rw
    environment:
      - RUST_LOG=info
      - DOCKER_HOST=unix:///var/run/docker.sock
    command:
      - "--bind-address"
      - "0.0.0.0:6443"
      - "--etcd-servers"
      - "http://rhino:2379"
      - "--tls"
      - "--tls-cert-file"
      - "/etc/kubernetes/pki/api-server.crt"
      - "--tls-key-file"
      - "/etc/kubernetes/pki/api-server.key"
      - "--skip-auth"
      - "--console-dir"
      - "/app/console"
      - "--log-level"
      - "info"

  # Scheduler - assigns pods to nodes
  scheduler:
    build:
      context: .
      dockerfile: services.Dockerfile
      target: scheduler
    container_name: rusternetes-scheduler
    depends_on:
      - rhino
      - api-server
    networks:
      - rusternetes-net
    environment:
      - RUST_LOG=info
    command: ["--etcd-servers", "http://rhino:2379", "--interval", "1"]

  # Controller Manager - runs controllers
  controller-manager:
    build:
      context: .
      dockerfile: services.Dockerfile
      target: controller-manager
    container_name: rusternetes-controller-manager
    depends_on:
      - rhino
      - api-server
    networks:
      - rusternetes-net
    volumes:
      - ./.rusternetes/certs:/etc/kubernetes/pki:ro
    environment:
      - RUST_LOG=info
    command: ["--etcd-servers", "http://rhino:2379", "--sync-interval", "5"]

  # Kubelet - node agent that manages containers
  kubelet:
    build:
      context: .
      dockerfile: services.Dockerfile
      target: kubelet
    container_name: rusternetes-kubelet
    privileged: true
    depends_on:
      - rhino
      - api-server
    networks:
      - rusternetes-net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - ${KUBELET_VOLUMES_PATH}:${KUBELET_VOLUMES_PATH}:rw
      - ./.rusternetes/certs:/root/.rusternetes/certs:ro
    environment:
      - RUST_LOG=info
      - RUST_MIN_STACK=8388608
      - DOCKER_HOST=unix:///var/run/docker.sock
      - KUBERNETES_SERVICE_HOST_OVERRIDE=api-server
      - KUBELET_VOLUMES_PATH=${KUBELET_VOLUMES_PATH}
    command: ["--node-name", "node-1", "--etcd-servers", "http://rhino:2379", "--cluster-dns", "10.96.0.10", "--metrics-port", "10250", "--sync-interval", "3"]

  # Kubelet 2 - second node agent for multi-node conformance tests
  kubelet2:
    build:
      context: .
      dockerfile: services.Dockerfile
      target: kubelet
    container_name: rusternetes-kubelet2
    privileged: true
    depends_on:
      - rhino
      - api-server
    networks:
      - rusternetes-net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - ${KUBELET_VOLUMES_PATH}:${KUBELET_VOLUMES_PATH}:rw
      - ./.rusternetes/certs:/root/.rusternetes/certs:ro
    environment:
      - RUST_LOG=info
      - RUST_MIN_STACK=8388608
      - DOCKER_HOST=unix:///var/run/docker.sock
      - KUBERNETES_SERVICE_HOST_OVERRIDE=api-server
      - KUBELET_VOLUMES_PATH=${KUBELET_VOLUMES_PATH}
    command: ["--node-name", "node-2", "--etcd-servers", "http://rhino:2379", "--cluster-dns", "10.96.0.10", "--metrics-port", "10250", "--sync-interval", "3"]

  # Kube-proxy - network proxy (host network mode for iptables)
  kube-proxy:
    build:
      context: .
      dockerfile: services.Dockerfile
      target: kube-proxy
    container_name: rusternetes-kube-proxy
    privileged: true
    network_mode: host
    user: "0:0"
    depends_on:
      - api-server
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - SYS_ADMIN
    environment:
      - RUST_LOG=info
    # Host network: rhino is accessible via the mapped port on localhost
    command: ["--node-name", "node-1", "--etcd-servers", "http://localhost:2379"]

networks:
  rusternetes-net:
    driver: bridge
    name: rusternetes-network

volumes:
  rhino-data:
    name: rusternetes-rhino-data
